@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager

@using FutureTechnologyE_Commerce.Models.ViewModels
@model HomeIndexViewModel
@{
    ViewData["Title"] = "Home Page";
}

<!-- Hero Carousel -->
<div class="hero-carousel fade-in mb-5">
    <div id="mainCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            <!-- Slide 1 -->
            <div class="carousel-item active">
                <div class="carousel-content">
                    <img src="/images/ASUS TUF Gaming F15 Gaming Laptop.jpeg" alt="ASUS TUF Gaming F15"
                         class="carousel-image">
                    <div class="carousel-overlay"></div>
                    <div class="carousel-caption">
                        <h2 class="display-4 fw-bold mb-3 animate__animated animate__fadeInDown">ASUS TUF Gaming F15</h2>
                        <p class="lead mb-4 animate__animated animate__fadeInUp">Experience unrivaled gaming performance with the latest technology</p>
                        <a asp-controller="Laptops" asp-action="Details" asp-route-id="@Model.Laptops.FirstOrDefault()?.ProductID" class="btn btn-primary btn-lg animate__animated animate__fadeInUp">
                            Explore Now <i class="bi bi-arrow-right ms-2"></i>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Slide 2 -->
            <div class="carousel-item">
                <div class="carousel-content">
                    <img src="/images/ASUS TUF GAMING F15.jpeg" alt="ASUS TUF Gaming Series" class="carousel-image">
                    <div class="carousel-overlay"></div>
                    <div class="carousel-caption">
                        <h2 class="display-4 fw-bold mb-3 animate__animated animate__fadeInDown">ASUS TUF Gaming Series</h2>
                        <p class="lead mb-4 animate__animated animate__fadeInUp">Built for durability, designed for victory</p>
                        <a asp-controller="Laptops" asp-action="Details" asp-route-id="@Model.Laptops.FirstOrDefault()?.ProductID" class="btn btn-primary btn-lg animate__animated animate__fadeInUp">
                            Learn More <i class="bi bi-arrow-right ms-2"></i>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Slide 3 -->
            <div class="carousel-item">
                <div class="carousel-content">
                    <img src="/images/Pc Portable Gamer ASUS TUF F15 _ 15_6″ _ Intel i7 _ RTX 4060 _ 8 Go Ram.jpeg"
                         alt="RTX 4060 Gaming Laptop" class="carousel-image">
                    <div class="carousel-overlay"></div>
                    <div class="carousel-caption">
                        <h2 class="display-4 fw-bold mb-3 animate__animated animate__fadeInDown">RTX 4060 Gaming Laptop</h2>
                        <p class="lead mb-4 animate__animated animate__fadeInUp">Powered by NVIDIA RTX 4060 for ultimate gaming performance</p>
                        <a asp-controller="Laptops" asp-action="Details" asp-route-id="@Model.Laptops.FirstOrDefault()?.ProductID" class="btn btn-primary btn-lg animate__animated animate__fadeInUp">
                            Shop Now <i class="bi bi-arrow-right ms-2"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Carousel Controls -->
        <button class="carousel-control-prev" type="button" data-bs-target="#mainCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#mainCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>

        <!-- Carousel Indicators -->
        <div class="carousel-indicators">
            <button type="button" data-bs-target="#mainCarousel" data-bs-slide-to="0" class="active"></button>
            <button type="button" data-bs-target="#mainCarousel" data-bs-slide-to="1"></button>
            <button type="button" data-bs-target="#mainCarousel" data-bs-slide-to="2"></button>
        </div>
    </div>
</div>

<!-- Best Selling Products Section -->
<section class="products-section py-5 fade-in">
    <div class="container">
        <div class="section-header mb-4">
            <h2 class="section-title">Best Seller</h2>
            <a asp-action="GetAllProducts" asp-controller="Home" class="btn btn-outline-primary">
                View All <i class="bi bi-arrow-right ms-2"></i>
            </a>
        </div>

        <div class="products-carousel">
            <div class="products-slick" data-nav="#products-slick-nav">
                @foreach (var product in Model.Products)
                {
                    <div class="product-item">
                        <div class="product-card h-100">
                            <div class="card shadow-hover">
                                <div class="card-img-wrapper">
                                    <img src="@product.ImageUrl" class="card-img-top" alt="@product.Name"
                                         onerror="this.src='/images/placeholder.avif'">
                                    <div class="product-overlay">
                                        @if (product.GetType().Name == "Laptop")
                                        {
                                            <a asp-controller="Laptops" asp-action="Details" asp-route-id="@product.ProductID" class="btn btn-light btn-sm">
                                                View Details
                                            </a>
                                        }
                                        else
                                        {
                                            <a asp-controller="Home" asp-action="Details" asp-route-id="@product.ProductID" class="btn btn-light btn-sm">
                                                View Details
                                            </a>
                                        }
                                    </div>
                                    @if (product.StockQuantity > 0)
                                    {
                                        <span class="badge bg-success position-absolute top-0 end-0 m-3">In Stock</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger position-absolute top-0 end-0 m-3">Sold Out</span>
                                    }
                                </div>
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title text-truncate-2">@product.Name</h5>
                                    <div class="price-section mb-3">
                                        <span class="current-price">@product.Price.ToString("N0") EGP</span>
                                        <span class="original-price">@((product.Price * 1.2m).ToString("N0")) EGP</span>
                                    </div>
                                    <div class="mt-auto">
                                        @if (SignInManager.IsSignedIn(User))
                                        {
                                            <form asp-controller="Cart" asp-action="AddToCart" method="post" class="add-to-cart-form">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="productId" value="@product.ProductID" />
                                                <button type="submit" class="btn btn-primary w-100"
                                                        @(product.StockQuantity == 0 ? "disabled" : "")>
                                                    <span class="spinner-border spinner-border-sm d-none" role="status"></span>
                                                    <span class="btn-text">Add to Cart</span>
                                                    <i class="bi bi-cart-plus ms-2"></i>
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <a asp-controller="Cart" asp-action="RequestAddToCartAfterLogin"
                                               asp-route-productId="@product.ProductID" class="btn btn-primary w-100">
                                                Add to Cart <i class="bi bi-cart-plus ms-2"></i>
                                            </a>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            <div id="products-slick-nav"></div>
        </div>
    </div>
</section>

<style>
    .hero-carousel {
        margin-top: -1rem;
        margin-bottom: 3rem;
    }

    .carousel-content {
        position: relative;
        height: 600px;
        overflow: hidden;
        border-radius: 12px;
    }

    .carousel-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transform: scale(1.1);
        transition: transform 6s ease-in-out;
    }

    .carousel-item.active .carousel-image {
        transform: scale(1);
    }

    .carousel-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(to right, rgba(0,0,0,0.7), rgba(0,0,0,0.3));
        border-radius: 12px;
    }

    .carousel-caption {
        bottom: 20%;
        text-align: left;
        max-width: 600px;
        margin: 0 auto;
        left: 10%;
        right: 10%;
    }

        .carousel-caption h2 {
            font-size: 3.5rem;
            font-weight: 700;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 1rem;
        }

        .carousel-caption .lead {
            font-size: 1.25rem;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
            margin-bottom: 2rem;
        }

    .carousel-indicators {
        bottom: 20px;
    }

        .carousel-indicators button {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin: 0 6px;
            background-color: rgba(255,255,255,0.5);
            border: none;
            transition: all 0.3s ease;
        }

            .carousel-indicators button.active {
                background-color: var(--primary-color);
                transform: scale(1.2);
            }

    /* Products Section */
    .products-section {
        padding: 4rem 0;
        margin-bottom: 2rem;
        background-color: var(--white);
        border-radius: 1rem;
        box-shadow: var(--shadow-sm);
    }

    .products-carousel {
        padding: 2rem 0;
        margin-bottom: 2rem;
    }

    .product-item {
        padding: 15px;
    }

    .card-img-wrapper {
        position: relative;
        overflow: hidden;
        aspect-ratio: 4/3;
        border-radius: 12px 12px 0 0;
    }

    .card-img-top {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }

    .product-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.4);
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        transition: opacity 0.3s ease;
        border-radius: 12px 12px 0 0;
    }

    .card:hover .product-overlay {
        opacity: 1;
    }

    .card:hover .card-img-top {
        transform: scale(1.1);
    }

    .card-body {
        padding: 1.5rem;
    }

    .card-title {
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: 1rem;
        color: var(--text-color);
    }

    .price-section {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 1rem;
    }

    .current-price {
        color: var(--text-color);
        font-weight: 600;
        font-size: 1.1rem;
    }

    .original-price {
        color: var(--text-muted);
        text-decoration: line-through;
        font-size: 0.9em;
    }

    .badge {
        background-color: rgba(46, 204, 113, 0.8);
        font-weight: 500;
        padding: 0.5em 1em;
        border-radius: 6px;
        backdrop-filter: blur(4px);
    }

        .badge.bg-danger {
            background-color: rgba(231, 76, 60, 0.8);
        }

    /* Slick Carousel Navigation */
    .slick-prev, .slick-next {
        width: 40px;
        height: 40px;
        background: rgba(255, 255, 255, 0.9);
        border-radius: 50%;
        box-shadow: 0 2px 5px var(--shadow-color);
        z-index: 1;
        transition: all 0.3s ease;
    }

        .slick-prev:hover, .slick-next:hover {
            background: var(--primary-color);
        }

        .slick-prev:before, .slick-next:before {
            color: var(--text-color);
            opacity: 0.7;
        }

        .slick-prev:hover:before, .slick-next:hover:before {
            color: white;
            opacity: 1;
        }

    .slick-prev {
        left: -20px;
    }

    .slick-next {
        right: -20px;
    }

    /* Section Header */
    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
    }

    .section-title {
        font-size: 2rem;
        font-weight: 600;
        color: var(--text-color);
        margin: 0;
    }

    /* Responsive Styles */
    @@media (max-width: 768px) {
        .carousel-content {
            height: 400px;
        }

        .carousel-caption {
            bottom: 10%;
            left: 5%;
            right: 5%;
        }

            .carousel-caption h2 {
                font-size: 2rem;
            }

            .carousel-caption .lead {
                font-size: 1rem;
            }

        .section-header {
            flex-direction: column;
            gap: 1rem;
            text-align: center;
        }

        .section-title {
            font-size: 1.5rem;
        }

        .products-carousel {
            padding: 0 20px;
        }

        .slick-prev {
            left: -10px;
        }

        .slick-next {
            right: -10px;
        }
    }
</style>



<!-- Laptops Section -->
<section class="products-section py-5 fade-in">
    <div class="container">
        <div class="section-header mb-4">
            <h2 class="section-title">Featured Laptops</h2>
            <a asp-action="GetAllLaptops" asp-controller="Home" class="btn btn-outline-primary">
                View All <i class="bi bi-arrow-right ms-2"></i>
            </a>
        </div>

        <div class="products-carousel">
            <div class="products-slick" data-nav="#laptops-slick-nav">
                @foreach (var laptop in Model.Laptops)
                {
                    <div class="product-item">
                        <div class="product-card h-100">
                            <div class="card shadow-hover">
                                <div class="card-img-wrapper">
                                    <img src="@laptop.ImageUrl" class="card-img-top" alt="@laptop.Name"
                                         onerror="this.src='/images/placeholder.avif'">
                                    <div class="product-overlay">
                                        <a asp-controller="Laptops" asp-action="Details" asp-route-id="@laptop.ProductID" class="btn btn-light btn-sm">
                                            View Details
                                        </a>
                                    </div>
                                    @if (laptop.StockQuantity > 0)
                                    {
                                        <span class="badge bg-success position-absolute top-0 end-0 m-3">In Stock</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger position-absolute top-0 end-0 m-3">Sold Out</span>
                                    }
                                </div>
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title text-truncate-2">@laptop.Name</h5>
                                    <div class="price-section mb-3">
                                        <span class="current-price">@laptop.Price.ToString("N0") EGP</span>
                                        <span class="original-price">@((laptop.Price * 1.2m).ToString("N0")) EGP</span>
                                    </div>
                                    <div class="mt-auto">
                                        @if (SignInManager.IsSignedIn(User))
                                        {
                                            <form asp-controller="Cart" asp-action="AddToCart" method="post" class="add-to-cart-form">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="productId" value="@laptop.ProductID" />
                                                <button type="submit" class="btn btn-primary w-100"
                                                        @(laptop.StockQuantity == 0 ? "disabled" : "")>
                                                    <span class="spinner-border spinner-border-sm d-none" role="status"></span>
                                                    <span class="btn-text">Add to Cart</span>
                                                    <i class="bi bi-cart-plus ms-2"></i>
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <a asp-controller="Cart" asp-action="RequestAddToCartAfterLogin"
                                               asp-route-productId="@laptop.ProductID" class="btn btn-primary w-100">
                                                Add to Cart <i class="bi bi-cart-plus ms-2"></i>
                                            </a>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            <div id="laptops-slick-nav"></div>
        </div>
    </div>
</section>


<section class="products-section py-5 fade-in">
    <div class="container">
        <div class="section-header mb-4">
            <h2 class="section-title">Accessories</h2>
            <a asp-action="GetAllAccessories" asp-controller="Home" class="btn btn-outline-primary">
                View All <i class="bi bi-arrow-right ms-2"></i>
            </a>
        </div>

        <div class="products-carousel">
            <div class="products-slick" data-nav="#products-slick-nav">
                @foreach (var product in Model.Accessories)
                {
                    <div class="product-item">
                        <div class="product-card h-100">
                            <div class="card shadow-hover">
                                <div class="card-img-wrapper">
                                    <img src="@product.ImageUrl" class="card-img-top" alt="@product.Name"
                                         onerror="this.src='/images/placeholder.avif'">
                                    <div class="product-overlay">
                                        @if (product.GetType().Name == "Laptop")
                                        {
                                            <a asp-controller="Laptops" asp-action="Details" asp-route-id="@product.ProductID" class="btn btn-light btn-sm">
                                                View Details
                                            </a>
                                        }
                                        else
                                        {
                                            <a asp-controller="Home" asp-action="Details" asp-route-id="@product.ProductID" class="btn btn-light btn-sm">
                                                View Details
                                            </a>
                                        }
                                    </div>
                                    @if (product.StockQuantity > 0)
                                    {
                                        <span class="badge bg-success position-absolute top-0 end-0 m-3">In Stock</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger position-absolute top-0 end-0 m-3">Sold Out</span>
                                    }
                                </div>
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title text-truncate-2">@product.Name</h5>
                                    <div class="price-section mb-3">
                                        <span class="current-price">@product.Price.ToString("N0") EGP</span>
                                        <span class="original-price">@((product.Price * 1.2m).ToString("N0")) EGP</span>
                                    </div>
                                    <div class="mt-auto">
                                        @if (SignInManager.IsSignedIn(User))
                                        {
                                            <form asp-controller="Cart" asp-action="AddToCart" method="post" class="add-to-cart-form">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="productId" value="@product.ProductID" />
                                                <button type="submit" class="btn btn-primary w-100"
                                                        @(product.StockQuantity == 0 ? "disabled" : "")>
                                                    <span class="spinner-border spinner-border-sm d-none" role="status"></span>
                                                    <span class="btn-text">Add to Cart</span>
                                                    <i class="bi bi-cart-plus ms-2"></i>
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <a asp-controller="Cart" asp-action="RequestAddToCartAfterLogin"
                                               asp-route-productId="@product.ProductID" class="btn btn-primary w-100">
                                                Add to Cart <i class="bi bi-cart-plus ms-2"></i>
                                            </a>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            <div id="products-slick-nav"></div>
        </div>
    </div>
</section>


@section Scripts {
    <script>
        $(document).ready(function () {
            // Initialize Slick Carousel for products
            $('.products-slick').slick({
                slidesToShow: 4,
                slidesToScroll: 1,
                arrows: true,
                dots: false,
                infinite: true,
                speed: 300,
                autoplay: true,
                autoplaySpeed: 3000,
                responsive: [
                    {
                        breakpoint: 1200,
                        settings: {
                            slidesToShow: 3
                        }
                    },
                    {
                        breakpoint: 992,
                        settings: {
                            slidesToShow: 2
                        }
                    },
                    {
                        breakpoint: 576,
                        settings: {
                            slidesToShow: 1
                        }
                    }
                ]
            });

            // Initialize Bootstrap Carousel
            var myCarousel = new bootstrap.Carousel(document.getElementById('mainCarousel'), {
                interval: 5000,
                wrap: true
            });

            // Add animation classes to elements when they come into view
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                    }
                });
            }, {
                threshold: 0.1
            });

            document.querySelectorAll('.fade-in').forEach((element) => {
                observer.observe(element);
            });
        });
    </script>
}